<html>
    <head><title>新增使用者</title></head>
    <body>
<?php
    // 關閉錯誤訊息顯示（不建議正式環境使用，會隱藏問題）
    error_reporting(0);

    // 啟用 session，用來判斷使用者是否登入
    session_start();

    // 如果沒有登入（session 中沒有 "id"），顯示提示並跳轉登入頁
    if (!$_SESSION["id"]) {
        echo "請登入帳號"; // 顯示提示訊息
        echo "<meta http-equiv=REFRESH content='3, url=2.login.html'>"; // 3秒後跳轉到登入頁面
    }
    else {
        // 如果已登入，顯示新增使用者的表單
        echo "
            <form action=15.user_add.php method=post>
                帳號：<input type=text name=id><br>          <!-- 輸入使用者帳號 -->
                密碼：<input type=text name=pwd><p></p>     <!-- 輸入使用者密碼（應改為 type=password） -->
                <input type=submit value=新增>              <!-- 提交表單，新增使用者 -->
                <input type=reset value=清除>              <!-- 清空輸入欄位 -->
            </form>
        ";
    }
?>
    </body>
</html>
